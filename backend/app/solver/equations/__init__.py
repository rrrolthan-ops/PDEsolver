"""Per-equation templates (heat, wave, Laplace, …).

Phase 1 only ships `heat.py` as a thin label module; the actual heat
logic lives in `solver.methods.separation_of_variables`. Later phases
will add the analogous modules for wave, Laplace, Poisson, etc.
"""
